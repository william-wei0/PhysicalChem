{
  "name": "physicalchem",
  "version": "1.0.0",
  "description": "This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.",
  "main": "index.js",
  "scripts": {
    "devServer": "npm run dev --prefix server",
    "devClient": "npm run dev --prefix client",
    "dev": "concurrently \"npm run devServer\" \"npm run devClient\"",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && npm audit fix && npm run build --prefix server",
    "start": "node server/dist/server.js",
    "preview": "npm run build && npm run start"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/william-wei0/physical-chem.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/william-wei0/physical-chem/issues"
  },
  "homepage": "https://github.com/william-wei0/physical-chem#readme",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
